# 4.2 Redis (umbrella)

- **Status:** `todo`
- **Decision:** Redis is the **only** runtime `Cache` (approach A). Session store for FSM, not a money cache. No `memory|redis` driver after cutover.
- **Design:** [`docs/superpowers/specs/2026-09-09-redis-cache-design.md`](../../superpowers/specs/2026-09-09-redis-cache-design.md)

Do **not** implement this umbrella as one session. Next pickup is `4.2.1`.

| Step | File | Goal |
| --- | --- | --- |
| 4.2.1 | [Foundation + adapter](4.02.1-redis-foundation.md) | Compose + CI Redis, `Open`, `ports.Cache`, integration tests |
| 4.2.2 | [Cutover](4.02.2-redis-cutover.md) | Wire `cmd/bot`, require `REDIS_URL`, keep `memorycache` for unit tests only |

**Later, not here:** managed Redis / TLS in the URL → [`4.03-hosting.md`](4.03-hosting.md). Token bucket, per-user lock, app replicas, optional snapshot cache → [`4.09-load.md`](4.09-load.md).
