# Persistence (current)

`cmd/bot` uses Postgres only. Trial/paywall **semantics** are [`billing.md`](billing.md). Schema and adapter: [`4.1.1`](../steps/4.01.1-postgres-foundation.md)–[`4.1.3`](../steps/4.01.3-postgres-cutover.md) ([umbrella](../steps/4.01-postgresql.md), [design](../../superpowers/specs/2026-09-09-postgresql-migration-design.md)). Managed instance, TLS, backups: [`4.3`](../steps/4.03-hosting.md). Partitioning: [`4.9`](../steps/4.09-load.md).

## Postgres adapter

`internal/adapter/postgres` implements the `service` repository and `Transactor` ports. `cmd/bot` opens it with `DATABASE_URL`.

| Concern | Choice |
| --- | --- |
| Driver | `github.com/jackc/pgx/v5/stdlib` |
| Open | `Open(ctx, url)` — pool, session timeouts, ping, embed migrate; ping/migrate failure returns an error |
| Pool | `MaxOpenConns = 10`, `MaxIdleConns = 5`, `ConnMaxLifetime = 30m`, `ConnMaxIdleTime = 5m` |
| Session | `statement_timeout = 5s`, `lock_timeout = 2s`, `idle_in_transaction_session_timeout = 10s` (every new connection) |
| Migrations | `schema_migrations` + transactional apply of `migrations/001_init.sql`; second `Open` is a no-op for `001_init` |
| IDs | `BIGINT`; bank/operation ids `GENERATED BY DEFAULT AS IDENTITY` |
| Timestamps | `TIMESTAMPTZ` |
| `include_in_total` | `BOOLEAN` |
| Uniqueness | `UNIQUE (user_id, name_normalized)` — still filled by `NormalizeBankName`, not `lower()` |
| FKs | `banks.user_id` / `operations.user_id` → `users` `ON DELETE CASCADE`; `operations.bank_id` `ON DELETE SET NULL` |
| Indexes | `banks (user_id)`; `operations (user_id, created_at DESC)`; `operations (bank_id)`; `users (last_activity_at)` |
| SQL | `$1` placeholders; `RETURNING` (no `LastInsertId`); `TIMESTAMPTZ` / `BOOLEAN` scan |
| Unique | `23505` → `domain.ErrBankNameTaken` |
| Row locks | `GetByID` / `GetByName` append `FOR UPDATE` **only** when `InTx` stored a `*sql.Tx` on the context. `List`, `Total`, and get outside a tx stay unlocked |
| `InTx` | begin/commit/rollback; **3** attempts on `40P01` and `40001` only |

Local DBs: Compose `postgres:16` creates `finbot` (bot) and `finbot_test` (integration). Process: [`platform.md`](platform.md).

Every SQL query that is per-user is filtered by `user_id`.

## users

- `telegram_id` BIGINT PRIMARY KEY
- `username` TEXT
- `last_activity_at` TIMESTAMPTZ
- `plan` TEXT NOT NULL DEFAULT `'trial'`
- `trial_ends_at` TIMESTAMPTZ; set once at insert
- `discount_percent` INTEGER NULL — `NULL` = not whitelisted; `0`–`100` = admin privilege (`100` = free)
- `created_at`, `updated_at` TIMESTAMPTZ
- `locale` TEXT NOT NULL DEFAULT `'en'` — Stage 3 rows are `en`. New signups insert empty until the `/start` language pick; `/language` updates it. Known codes: `en`, `ru`, `uk`, `md`.
- `referred_by` BIGINT NULL — Telegram id of the referrer; set once on first `/start` payload

## banks

- `id` BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY
- `user_id` BIGINT NOT NULL REFERENCES users
- `name` TEXT NOT NULL
- `balance_cents` BIGINT NOT NULL DEFAULT `0`
- `include_in_total` BOOLEAN NOT NULL
- `created_at`, `updated_at` TIMESTAMPTZ
- UNIQUE `(user_id, name_normalized)` extra column, filled by the app with `NormalizeBankName`
- `currency` TEXT NOT NULL — default from `DEFAULT_CURRENCY` (e.g. `USD`); hidden in Stage 3 copy

## operations

Append-only. Written by service mutators; **no user-facing `/history` until `4.10`**.

- `id` BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY
- `user_id` BIGINT NOT NULL REFERENCES users
- `bank_id` BIGINT NULL — nullable when the bank was deleted
- `type` TEXT NOT NULL — `add` / `spend` / `set` / `delete` / `transfer` / `rename`
- `amount_cents` BIGINT NOT NULL
- `balance_after_cents` BIGINT NULL — N/A for delete
- `meta` TEXT — bank name as typed on add/spend/set/delete; `Old -> New` on rename; counterpart bank id for transfer. Empty stored as SQL NULL.
- `created_at` TIMESTAMPTZ

Delete operation: `amount_cents` is last balance, `balance_after_cents` null, `meta` is the bank name as typed.

## Total

```sql
SELECT COALESCE(SUM(balance_cents), 0)
FROM banks
WHERE user_id = $1 AND include_in_total = TRUE;
```
